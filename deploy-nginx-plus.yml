---
- hosts: all
  collections:
    - nginxinc.nginx_core
  roles:
    - role: nginx
      vars:
        nginx_type: plus
        nginx_license:
          certificate: /var/lib/awx/projects/nginx-repo.crt
          key: /var/lib/awx/projects/nginx-repo.key
